$(document).ready(function() {

    //create the bingo board
    var board = new Array(5);
    for (var i = 0; i < board.length; i++) {
        board[i] = new Array(5);
    }

    var wordList = Array(24);


    //prototype the cells
    var bingo_Cell = {
        //checked (boolean)
        //text
    };

    //register events for listeners
});